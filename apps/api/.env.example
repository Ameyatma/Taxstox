# TaxStox Backend — Environment Variables
# Copy this to .env for local development

# JWT secret key (change in production!)
TAXSTOX_JWT_SECRET=taxstox-dev-secret-change-in-production

# Database path (defaults to ./data/taxstox.db)
# TAXSTOX_DB_PATH=/app/data/taxstox.db
